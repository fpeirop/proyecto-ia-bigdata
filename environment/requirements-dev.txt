# --- Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0    # Necesario para el reporte de cobertura en el CI

# --- Calidad de Código (Linting & Formatting) ---
black>=23.7.0        # Requerido por el paso "Comprobar formato"
ruff>=0.0.280        # Requerido por el paso "Ejecutar linter Ruff"
isort>=5.12.0        # Para organizar imports según .pre-commit-config.yaml

# --- Hooks ---
pre-commit>=3.3.0    # Para gestionar el archivo .pre-commit-config.yaml
